CREATE TABLE IF NOT EXISTS players (
                                       player_name VARCHAR(10),
                                       top_score INTEGER
);

INSERT INTO players (player_name, top_score)
VALUES ('Martin', 1234);